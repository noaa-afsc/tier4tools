% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spr_inputs.R
\name{plot_spr_inputs}
\alias{plot_spr_inputs}
\title{Plot SPR input assumptions}
\usage{
plot_spr_inputs(
  x,
  species = NULL,
  compare = NULL,
  panels = c("mat_selex", "wt", "len", "M"),
  overlay_maturity_selex = TRUE,
  show_fleets = FALSE,
  return_data = FALSE
)
}
\arguments{
\item{x}{An object returned by spr_input().}

\item{species}{Character vector of species names to plot. Defaults to all in x$species.}

\item{compare}{Optional named list of additional spr_input objects (e.g.,
sensitivities). Each element name is used as the scenario label in
legends/facets. Example: compare = list(Base = inp_base, Sens1 = inp_sens)}

\item{panels}{Character vector specifying which panels to draw. Any of:
c("mat_selex","wt","len","M")}

\item{overlay_maturity_selex}{Logical. If TRUE, maturity and selectivity are overlayed
on a single plot (recommended).}

\item{show_fleets}{Logical. If TRUE, and if fleet-specific selectivity is available,
overlays fleet selectivity curves and the effective selectivity curve. Default FALSE.}

\item{return_data}{Logical. If TRUE, returns a list with plots and the tidy data used.}
}
\value{
By default, returns a named list of ggplot objects. If return_data=TRUE,
returns list(plots=..., data=...).
}
\description{
Visualize inputs supplied to spr_input() object for checking assumptions and
reporting (base vs sensitivity, or across species). By default, makes:
\itemize{
\item maturity and selectivity on the same panel
\item weight-at-age
Optional panels include length-at-age and M-at-age.
}
}
